import 'package:flutter/material.dart';

class HomeDiys extends StatelessWidget{
  const HomeDiys({super.key});
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(child: Text("home diiys"))
    );
  }
}